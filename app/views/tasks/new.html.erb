<% content_for :title, "New Task" %>
<h1 class="text-2xl font-bold text-hamzis-brown dark:text-hamzis-copper mb-4">
  <% if @project %>
    New Task for <%= @project.name %>
  <% else %>
    New Task
  <% end %>
</h1>
<%= render "form", project: @project, task: @task %>
<div class="mt-4">
  <%= link_to "← Back", (@project ? project_tasks_path(@project) : tasks_path),
      class: "px-3 py-1 rounded bg-hamzis-brown text-hamzis-white hover:bg-hamzis-copper transition" %>
</div>
