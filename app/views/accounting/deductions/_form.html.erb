<%= form_with(model: @deduction, local: true, class: "space-y-6") do |f| %>
  <% if @deduction.errors.any? %>
    <div class="bg-red-100 text-red-700 p-4 rounded">
      <h2 class="font-bold mb-2"><%= pluralize(@deduction.errors.count, "error") %> prevented this deduction from being saved:</h2>
      <ul class="list-disc pl-5">
        <% @deduction.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <!-- Salary -->
  <div>
    <%= f.label :salary_id, "Salary", class: "block text-sm font-medium mb-1" %>
    <%= f.collection_select :salary_id, Accounting::Salary.all, :id, :id,
          { prompt: "Select a salary" },
          { class: "w-full px-3 py-2 border rounded focus:ring-hamzis-copper focus:border-hamzis-copper" } %>
  </div>
  <!-- Deduction Type -->
  <div>
    <%= f.label :deduction_type, "Type", class: "block text-sm font-medium mb-1" %>
    <%= f.select :deduction_type, Accounting::Deduction.deduction_types.keys.map { |t| [t.humanize, t] },
          {}, { class: "w-full px-3 py-2 border rounded focus:ring-hamzis-copper focus:border-hamzis-copper" } %>
  </div>
  <!-- Amount -->
  <div>
    <%= f.label :amount, class: "block text-sm font-medium mb-1" %>
    <%= f.number_field :amount, step: "0.01", class: "w-full px-3 py-2 border rounded focus:ring-hamzis-copper focus:border-hamzis-copper" %>
  </div>
  <!-- Notes -->
  <div>
    <%= f.label :notes, "Notes (optional)", class: "block text-sm font-medium mb-1" %>
    <%= f.text_area :notes, rows: 3, class: "w-full px-3 py-2 border rounded focus:ring-hamzis-copper focus:border-hamzis-copper" %>
  </div>
  <!-- Submit -->
  <div class="flex justify-end">
    <%= f.submit class: "px-4 py-2 rounded bg-hamzis-copper text-hamzis-white hover:bg-hamzis-black transition" %>
  </div>
<% end %>
