<% content_for :title, "TXN: #{@transaction.reference.presence || @transaction.id} | Financial Ledger" %>
<div class="bg-brand-white/30 dark:bg-brand-black/30 backdrop-blur-md border border-brand-secondary/20 rounded-2xl shadow-2xl p-8 mb-10 relative overflow-hidden animate-fade-in">
    <% status_color = @transaction.status_paid? ? 'bg-green-500' : 'bg-brand-primary' %>
    <div class="absolute top-0 left-0 w-full h-1 <%= status_color %> opacity-40"></div>
    <div class="flex flex-col md:flex-row justify-between items-start gap-6">
        <div class="flex-1">
            <div class="flex items-center gap-3 mb-2">
                <span class="px-2 py-0.5 rounded bg-brand-secondary/10 border border-brand-secondary/20 text-[9px] font-black uppercase tracking-[0.2em] text-brand-secondary dark:text-brand-primary">
                    Reference #<%= @transaction.reference.presence || @transaction.id %>
                </span>
                <span class="inline-flex items-center gap-1.5 text-[9px] font-black uppercase tracking-widest text-brand-secondary/50 dark:text-brand-primary/50">
                    <span class="h-1.5 w-1.5 rounded-full <%= status_color %> <%= 'animate-pulse' unless @transaction.status_paid? %>"></span>
                    <%= @transaction.status.humanize %>
                </span>
            </div>
            <h2 class="text-3xl font-black text-brand-secondary dark:text-white uppercase tracking-tighter italic mb-4">
                <%= @transaction.description %>
            </h2>
            <% if @transaction.notes.present? %>
            <p class="text-sm text-brand-secondary/70 dark:text-white/70 leading-relaxed max-w-2xl italic border-l-2 border-brand-primary/30 pl-4">
                <%= @transaction.notes %>
            </p>
            <% end %>
        </div>
        <div class="flex flex-wrap md:flex-nowrap gap-3">
            <%= link_to edit_accounting_transaction_path(@transaction),
          class: "flex items-center gap-2 px-5 py-2.5 rounded-xl bg-brand-primary text-white text-[10px] font-black uppercase tracking-widest shadow-lg hover:bg-brand-black transition-all active:scale-95" do %>
            Edit Entry
            <% end %>
            <%= link_to accounting_transactions_path,
          class: "flex items-center gap-2 px-5 py-2.5 rounded-xl bg-brand-secondary/10 text-brand-secondary dark:text-white border border-brand-secondary/20 text-[10px] font-black uppercase tracking-widest hover:bg-brand-secondary hover:text-white transition-all" do %>
            Back to Ledger
            <% end %>
            <% if policy(@transaction).destroy? %>
            <%= button_to "#",
            class: "p-2.5 rounded-xl bg-red-500/10 text-red-600 border border-red-500/20 hover:bg-red-600 hover:text-white transition-all cursor-pointer",
            form: {
              "data-action" => "click->confirm-delete#open",
              "data-confirm-delete-title-param" => "Delete Entry?",
              "data-confirm-delete-message-param" => "Are you sure you want to delete this #{@transaction.transaction_type}? This will remove it from the balance sheet.",
              "data-confirm-delete-confirm-path-param" => accounting_transaction_path(@transaction)
            } do %>
            <svg xmlns="http://www.w3.org" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
            </svg>
            <% end %>
            <% end %>
        </div>
    </div>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 mt-10 pt-8 border-t border-brand-secondary/10">
        <div>
            <p class="text-[9px] font-black uppercase tracking-[0.2em] text-brand-primary mb-2">Entry Date</p>
            <p class="text-sm font-black text-brand-secondary dark:text-white font-mono tracking-tighter">
                <%= @transaction.date.strftime("%d.%m.%Y") %>
            </p>
        </div>
        <div>
            <p class="text-[9px] font-black uppercase tracking-[0.2em] text-brand-primary mb-2">Transaction Type</p>
            <p class="text-sm font-bold text-brand-secondary dark:text-white uppercase tracking-tight">
                <%= @transaction.transaction_type.humanize %>
            </p>
        </div>
        <div>
            <p class="text-[9px] font-black uppercase tracking-[0.2em] text-brand-primary mb-2">Total Amount</p>
            <p class="text-xl font-black text-brand-secondary dark:text-white font-mono tracking-tighter">
                <%= number_to_currency(@transaction.amount, unit: "₦") %>
            </p>
        </div>
        <div>
            <p class="text-[9px] font-black uppercase tracking-[0.2em] text-brand-primary mb-2">Logged By</p>
            <div class="flex items-center gap-2">
                <div class="h-5 w-5 rounded bg-brand-primary/10 flex items-center justify-center text-[8px] font-black text-brand-primary">
                    SY
                </div>
                <span class="text-[11px] font-bold text-brand-secondary dark:text-white uppercase tracking-tight">
                    System Entry
                </span>
            </div>
        </div>
    </div>
</div>