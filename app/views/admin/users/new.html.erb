<% content_for :title, "Provision User | Hamzis Systems" %>
<div class="mb-8">
  <nav class="flex items-center gap-2 text-[10px] font-bold uppercase tracking-widest text-hamzis-brown/50 dark:text-hamzis-copper/50 mb-2">
    <%= link_to "Security Center", dashboard_home_path, class: "hover:text-hamzis-copper transition-colors" %>
    <svg xmlns="http://www.w3.org" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7" /></svg>
    <%= link_to "User Registry", admin_users_path, class: "hover:text-hamzis-copper transition-colors" %>
    <svg xmlns="http://www.w3.org" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M9 5l7 7-7 7" /></svg>
    <span class="text-hamzis-copper text-xs">New Provision</span>
  </nav>
  <div class="flex items-center gap-3">
    <div class="p-3 rounded-2xl bg-hamzis-copper text-white shadow-lg shadow-hamzis-copper/20">
      <svg xmlns="http://www.w3.org" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" /></svg>
    </div>
    <div>
      <h1 class="text-3xl font-black tracking-tighter text-hamzis-brown dark:text-hamzis-copper">Credential Provisioning</h1>
      <p class="text-sm text-hamzis-brown/60 dark:text-hamzis-copper/60 italic">Granting system access privileges to a new network operator.</p>
    </div>
  </div>
</div>
<%= form_with(model: @user, url: admin_users_path, local: true, class: "space-y-6 hr-form") do |f| %>
  <%= render "form", f: f %>
<% end %>
